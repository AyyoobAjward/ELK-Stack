input {
  http {
    port => 8080
    codec => json
  }
}

filter {
}

output {
  elasticsearch {
    index => "nestjs-%{+YYYY.MM.dd}"
    hosts => "${ELASTIC_HOSTS}"
    user => "elastic"
    password => "${ELASTIC_PASSWORD}"
    cacert => "certs/ca/ca.crt"
  }
}
